global:
  scrape_interval: 15s

scrape_configs:
  - job_name: flask
    static_configs:
      - targets:
          - flask:5000

#This tells Prometheus: every 15 seconds, go to flask:5000/metrics and collect all the numbers.
